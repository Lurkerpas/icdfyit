<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="using:IcdFyIt.App.ViewModels"
        xmlns:ctrl="using:IcdFyIt.App.Controls"
        x:Class="IcdFyIt.App.Views.MemoriesWindow"
        Icon="avares://IcdFyIt.App/Assets/icon.png"
        Title="Memories"
        Width="1200" Height="640"
        MinWidth="700" MinHeight="400">

    <LayoutTransformControl>
        <LayoutTransformControl.LayoutTransform>
            <ScaleTransform ScaleX="{DynamicResource AppScale}" ScaleY="{DynamicResource AppScale}" />
        </LayoutTransformControl.LayoutTransform>
    <DockPanel>

        <!-- ── Toolbar ────────────────────────────────────────────────────────── -->
        <Border DockPanel.Dock="Top" Background="#2D2D30" Padding="0,1">
            <DockPanel LastChildFill="False">

                <!-- Left: action / column / data menus -->
                <Menu DockPanel.Dock="Left" Background="Transparent" VerticalAlignment="Center">

                    <!-- Edit actions -->
                    <MenuItem Header="Edit">
                        <MenuItem Header="Add…"      Command="{Binding AddCommand}"/>
                        <MenuItem Header="Duplicate" Command="{Binding DuplicateCommand}"/>
                        <MenuItem Header="Remove"    Command="{Binding RemoveCommand}"/>
                    </MenuItem>

                    <!-- Column visibility toggles -->
                    <MenuItem Header="Columns">
                        <MenuItem Header="Numeric ID"   x:Name="NumericIdItem"
                                  ToggleType="CheckBox" IsChecked="True"
                                  Click="OnColumnMenuItemChanged"/>
                        <MenuItem Header="Mnemonic"     x:Name="MnemonicItem"
                                  ToggleType="CheckBox" IsChecked="True"
                                  Click="OnColumnMenuItemChanged"/>
                        <MenuItem Header="Size"         x:Name="SizeItem"
                                  ToggleType="CheckBox" IsChecked="True"
                                  Click="OnColumnMenuItemChanged"/>
                        <MenuItem Header="Address"      x:Name="AddressItem"
                                  ToggleType="CheckBox" IsChecked="True"
                                  Click="OnColumnMenuItemChanged"/>
                        <MenuItem Header="Description"  x:Name="DescriptionItem"
                                  ToggleType="CheckBox" IsChecked="True"
                                  Click="OnColumnMenuItemChanged"/>
                        <MenuItem Header="Alignment"    x:Name="AlignmentItem"
                                  ToggleType="CheckBox" IsChecked="True"
                                  Click="OnColumnMenuItemChanged"/>
                        <MenuItem Header="Writable"     x:Name="WritableItem"
                                  ToggleType="CheckBox" IsChecked="True"
                                  Click="OnColumnMenuItemChanged"/>
                        <MenuItem Header="Readable"     x:Name="ReadableItem"
                                  ToggleType="CheckBox" IsChecked="True"
                                  Click="OnColumnMenuItemChanged"/>
                    </MenuItem>

                    <!-- CSV import / export -->
                    <MenuItem Header="Data">
                        <MenuItem Header="Export CSV" Command="{Binding ExportCsvCommand}"/>
                        <MenuItem Header="Import CSV" Command="{Binding ImportCsvCommand}"/>
                    </MenuItem>

                </Menu>

                <!-- Separator -->
                <Separator DockPanel.Dock="Left" Margin="4,4" Width="1" Background="#555"/>

                <!-- Filter by name / mnemonic -->
                <TextBlock DockPanel.Dock="Left" Text="Filter:" VerticalAlignment="Center"
                           Foreground="#CCCCCC" Margin="6,0,4,0"/>
                <TextBox   DockPanel.Dock="Left"
                           Text="{Binding FilterText, Mode=TwoWay}"
                           PlaceholderText="Search name or mnemonic…"
                           Width="200" Margin="0,2,8,2" VerticalAlignment="Center"/>

            </DockPanel>
        </Border>

        <!-- ── Spreadsheet grid ───────────────────────────────────────────────── -->
        <ctrl:DraggableGrid x:Name="MemoriesGrid"
                            ItemsSource="{Binding FilteredRows}"
                            SelectedItem="{Binding SelectedRow, Mode=TwoWay}">

            <ctrl:DraggableGrid.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Add…"      Command="{Binding AddCommand}"/>
                    <MenuItem Header="Duplicate" Command="{Binding DuplicateCommand}"/>
                    <MenuItem Header="Remove"    Command="{Binding RemoveCommand}"/>
                </ContextMenu>
            </ctrl:DraggableGrid.ContextMenu>

            <ctrl:DraggableGrid.Columns>

                <!-- Drag handle -->
                <ctrl:DraggableGridColumn ColumnType="DragHandle" Width="24"/>

                <!-- Name -->
                <ctrl:DraggableGridColumn Header="Name"
                                          Path="Name"
                                          Width="150"
                                          IsEditable="True"/>

                <!-- Numeric ID -->
                <ctrl:DraggableGridColumn Header="Numeric ID"
                                          Path="NumericId"
                                          Width="85"
                                          IsEditable="True"
                                          Name="NumericId"/>

                <!-- Mnemonic -->
                <ctrl:DraggableGridColumn Header="Mnemonic"
                                          Path="Mnemonic"
                                          Width="110"
                                          IsEditable="True"
                                          Name="Mnemonic"/>

                <!-- Size -->
                <ctrl:DraggableGridColumn Header="Size"
                                          Path="Size"
                                          Width="80"
                                          IsEditable="True"
                                          Name="Size"/>

                <!-- Address -->
                <ctrl:DraggableGridColumn Header="Address"
                                          Path="Address"
                                          Width="110"
                                          IsEditable="True"
                                          Name="Address"/>

                <!-- Description -->
                <ctrl:DraggableGridColumn Header="Description"
                                          Path="Description"
                                          Width="180"
                                          IsEditable="True"
                                          Name="Description"/>

                <!-- Alignment -->
                <ctrl:DraggableGridColumn Header="Alignment"
                                          Path="Alignment"
                                          Width="80"
                                          IsEditable="True"
                                          Name="Alignment"/>

                <!-- Is Writable -->
                <ctrl:DraggableGridColumn Header="Writable"
                                          Path="IsWritable"
                                          Width="75"
                                          ColumnType="Checkbox"
                                          IsEditable="True"
                                          Name="IsWritable"/>

                <!-- Is Readable -->
                <ctrl:DraggableGridColumn Header="Readable"
                                          Path="IsReadable"
                                          Width="75"
                                          ColumnType="Checkbox"
                                          IsEditable="True"
                                          Name="IsReadable"/>

            </ctrl:DraggableGrid.Columns>
        </ctrl:DraggableGrid>

    </DockPanel>
    </LayoutTransformControl>
</Window>
